# radiodish
Asyncio-native actor library for Python, built on a from-scratch RADIO-DISH implementation over stdlib UDP. Self-discovering, broker-less peer mesh with automatic wiring — no message broker, no manual endpoint config.
